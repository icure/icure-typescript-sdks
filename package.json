{
  "name": "icure-typescript-sdks",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "yarn nx run-many -t build -t test --verbose",
    "marshall": "yarn nx run-many -t marshall --verbose",
    "build": "yarn nx run-many -t build --parallel=5 --verbose"
  },
  "private": true,
  "devDependencies": {
    "nx": "16.3.2",
    "prettier": "^2.8.8",
    "pretty-quick": "^3.1.3",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "publishConfig": {
    "registry": "http://localhost:4873"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.6.0"
}
