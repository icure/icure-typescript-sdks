{
  "name": "icure-typescript-sdks",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "yarn nx run-many -t test --parallel=5 --verbose",
    "marshall": "yarn nx run-many -t marshall --parallel=5 --verbose",
    "build": "yarn nx run-many -t build --parallel=5 --verbose"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "16.3.2",
    "prettier": "^2.8.8",
    "pretty-quick": "^3.1.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
